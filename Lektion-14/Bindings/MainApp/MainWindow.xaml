<Window x:Class="MainApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MainApp"
        xmlns:viewmodels="clr-namespace:MainApp.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="400">
    <Grid Margin="20">

        <StackPanel>
            <TextBlock Text="Name" />
            <TextBox Text="{Binding CurrentItem.Name }"/>
            <TextBlock Text="{Binding InvalidName}" />

            <TextBlock Text="Description" Margin="0,10,0,0" />
            <TextBox Text="{Binding CurrentItem.Description }" />
            <TextBlock Text="{Binding InvalidDescription}" />

            <TextBlock Text="Price" Margin="0,10,0,0" />
            <TextBox Text="{Binding CurrentItem.Price }" />
            <TextBlock Text="{Binding InvalidPrice}" />

            <Button Content="SAVE" Command="{Binding SaveCommand}" Padding="5" Margin="0,10,0,0"  />
        </StackPanel>
        
    </Grid>
</Window>
